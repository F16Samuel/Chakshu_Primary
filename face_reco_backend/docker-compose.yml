version: '3'
services:
  app:
    build: .
    container_name: face-recog-container
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    ports:
      - 8000:8000
    volumes:
      - type: volume
        source: img_db
        target: /app/app/data
volumes:
  img_db:
